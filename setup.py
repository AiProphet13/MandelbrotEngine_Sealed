from setuptools import setup, find_packages

setup(
    name="mandelbrot_engine",
    version="1.0.0",
    description="Recursive Mandelbrot Sealer for mission authentication",
    author="Isaac El Yshwaty",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.7",
)
